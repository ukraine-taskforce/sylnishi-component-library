import React from "react";
import { render } from "@testing-library/react";

import Spacer from "./Spacer";

describe("Button", () => {
  it("renders the Button component", () => {
    render(<Spacer></Spacer>);
  });
});
